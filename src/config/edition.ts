import logo from '../images/pnh_logo.svg';

/** Branding and initial public provider scope belong to the same edition. */
export const edition = { logo, logoAlt: 'Logo Provincie Noord-Holland', publicOperators: ['voi'] };
